<div class="container shadow p-2 mb-3 bg-body-tertiary rounded div-max-width">
  <h5 class="text-center">Lotes</h5>
</div>
<div class="container div-max-width p-2 mb-3">
  <%= link_to 'Novo Lote', new_lot_path, class: 'btn btn-secondary' %>
</div>
<div class="container shadow bg-body-tertiary rounded div-max-width">
  <h5 class="text-center">Aguardando aprovação</h5>
</div>
<div class="container div-max-width" >
  <% if @waiting_approval_lots.any? %>
    <table class="table table-striped table-hover">
      <thead>
        <tr class="text-center">
          <th>Código</th>
          <th>Data inicial</th>
          <th>Data limite</th>
          <th>Lance mínimo</th>
          <th>Status</th>
        </tr>
      </thead>
      <tbody class="table-group-divider">
        <% @waiting_approval_lots.each do |lot| %>
          <tr class="text-center">
            <td><%= lot.code %></td>
            <td><%= I18n.localize(lot.start_date.to_date) %></td>
            <td><%= I18n.localize(lot.limit_date.to_date) %></td>
            <td><%= lot.minimum_bid %></td>
            <td><%= I18n.t(lot.status) %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  <% else %>
    <p>Não existem lotes no momento.</p>
  <% end %>
</div>

<div class="container shadow bg-body-tertiary rounded div-max-width">
  <h5 class="text-center">Aprovados - em andamento</h5>
</div>
<div class="container div-max-width" >
  <% if @approved_in_progress_lots.any? %>
    <table class="table table-striped table-hover">
      <thead>
        <tr class="text-center">
          <th>Código</th>
          <th>Data inicial</th>
          <th>Data limite</th>
          <th>Lance mínimo</th>
          <th>Status</th>
        </tr>
      </thead>
      <tbody class="table-group-divider">
        <% @approved_in_progress_lots.each do |lot| %>
          <tr class="text-center">
            <td><%= lot.code %></td>
            <td><%= I18n.localize(lot.start_date.to_date) %></td>
            <td><%= I18n.localize(lot.limit_date.to_date) %></td>
            <td><%= lot.minimum_bid %></td>
            <td><%= I18n.t(lot.status) %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  <% else %>
    <p>Não existem lotes no momento.</p>
  <% end %>
</div>

<div class="container shadow bg-body-tertiary rounded div-max-width">
  <h5 class="text-center">Aprovados - futuros</h5>
</div>
<div class="container div-max-width" >
  <% if @approved_future_lots.any? %>
    <table class="table table-striped table-hover">
      <thead>
        <tr class="text-center">
          <th>Código</th>
          <th>Data inicial</th>
          <th>Data limite</th>
          <th>Lance mínimo</th>
          <th>Status</th>
        </tr>
      </thead>
      <tbody class="table-group-divider">
        <% @approved_future_lots.each do |lot| %>
          <tr class="text-center">
            <td><%= lot.code %></td>
            <td><%= I18n.localize(lot.start_date.to_date) %></td>
            <td><%= I18n.localize(lot.limit_date.to_date) %></td>
            <td><%= lot.minimum_bid %></td>
            <td><%= I18n.t(lot.status) %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  <% else %>
    <p>Não existem lotes no momento.</p>
  <% end %>
</div>