<div class="container shadow p-2 mb-4 bg-body-tertiary rounded div-max-width">
  <h5 class="text-center">Produtos</h5>
</div>

<div class="container div-max-width p-2">
  <%= link_to 'Novo Produto', new_product_path, class: 'btn btn-secondary' %>
</div>
<div class="container div-max-width p-2" >
  <% if @products.any? %>
  <table class="table table-striped table-hover  mt-4 ">
    <thead>
      <tr class="text-center">
        <th>Código</th>
        <th>Nome</th>
        <th>Categoria</th>
      </tr>
    </thead>
    <tbody class="table-group-divider">
      <% @products.each do |product| %>
        <tr class="text-center">
          <td><%= link_to product.code, product_path(product), class: "link-secondary" %></td>
          <td><%= product.name %></td>
          <td><%= product.category.name %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
  <% else %>
  <p>Não existem produtos cadastrados.</p>
  <% end %>
</div>
